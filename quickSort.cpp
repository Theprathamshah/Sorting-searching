#include <bits/stdc++.h>
using namespace std;

int paratationArray(int l,int r,int *arr)
{
    int pivot = arr[r];
    int j = 0;
    for (int i = 0; i < r; i++)
    {
        
    }
    
}

int main()
{
    int n;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }


    return 0;
}
